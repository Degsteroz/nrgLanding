import {
  LOGO_IMAGE,
  MANSION,
  FOREST,
} from '@/config/config';

const headerConfig = {
  header: 'Ролевошная',
  logoImageSrc: LOGO_IMAGE,
  mansion: MANSION,
  forest: FOREST
};

export default headerConfig;
